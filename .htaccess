RewriteEngine On
RewriteBase /
RewriteRule ^index\.html$ - [L]

# If route does not exist → always return index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule . /index.html [L]
